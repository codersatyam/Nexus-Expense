import { Slot } from 'expo-router';
import React from 'react';

/**
 * Root navigator for the app
 * This is a wrapper component that provides the Expo Router navigation system
 */
export default function AppNavigator() {
  return <Slot />;
}
